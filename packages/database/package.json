{
  "name": "@repo/database",
  "version": "0.0.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "scripts": {
    "clean": "rm -rf ./dist && rm -rf ./src/generated",
    "build": "tsc --build",
    "db:generate": "pnpm clean && prisma generate && pnpm build",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "pg": "^8.18.0"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:^",
    "@types/node": "^25.2.2",
    "@types/pg": "^8.16.0",
    "prisma": "^7.3.0"
  }
}
